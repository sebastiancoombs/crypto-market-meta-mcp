{
  "name": "@money-tree/crypto-market-meta-mcp",
  "version": "0.1.0",
  "description": "x402 META: cross-feed crypto consensus price across coinpaprika + coincap + coinlore + cryptocompare + kraken. Pay-per-call USDC on Base.",
  "private": true,
  "type": "module",
  "main": "src/worker.js",
  "scripts": {
    "wrangler:dev": "wrangler dev",
    "wrangler:deploy": "wrangler deploy",
    "wrangler:dry-run": "wrangler deploy --dry-run --outdir=.wrangler-dryrun"
  },
  "dependencies": {
    "@coinbase/x402": "^2.1.0",
    "hono": "^4.6.0",
    "x402-hono": "^1.2.0"
  }
}
